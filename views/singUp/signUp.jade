extends ../layout

block content
  div.wrap-signup-o
   div.wrap-signup
    div.wrap-list
     form(method="post" name="list" action='singup' autocomplete="off")
      input(type="text" placeholder="昵称" name="nick" autocomplete="off").lorem
      input(type="password" placeholder="密码" name="pwd" autocomplete="off")
      span(style="color:#f00;display:block;margin:0 auto 10px;text-align:center;")=err?"密码太长":""
      div.denglubtn
       a(href='/') 登录>>
      input(type="submit").submit=确定
  script(src="./javascripts/signin.js")
